-- =====================================================================
-- Rollback: изтриване на импорт Book2023 (продукти + продажби)
-- =====================================================================
-- Пусни ПРЕДИ повторен import на 0007_book2023_historical_sales.sql
-- НЕ пипа контактите — само историческите продукти и sale work_items.
-- =====================================================================

BEGIN;

-- Продажби от импорта (по бележка)
DELETE FROM public.work_items wi
WHERE wi.event_code = 'sale'
  AND wi.notes LIKE 'Импорт Book2023, ред %';

-- Продукти създадени от импорта (slug pattern)
DELETE FROM public.products p
WHERE p.slug LIKE 'book2023-%';

COMMIT;
